import React from 'react'
import ReactDOM from 'react-dom/client'
import { PokemonBuscador } from './PokemonBuscador'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PokemonBuscador />
  </React.StrictMode>,
)
